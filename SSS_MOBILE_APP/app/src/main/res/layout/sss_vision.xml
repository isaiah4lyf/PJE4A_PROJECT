<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/background"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.example.isaia.sss_mobile_app.Audio_Recorder.VisualizerView
        android:id="@+id/visualizer3"
        android:layout_below="@+id/textView2"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginTop="@dimen/spacing_large"
        android:background="@drawable/visualizer"
        app:numColumns="100"
        app:renderColor="@color/btnRequest"
        app:renderRange="both"
        app:renderType="pixel" />
    <ImageButton
        android:id="@+id/start"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:background="@xml/round_button"
        android:src="@drawable/ic_mic_black_24dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:layout_marginRight="5dp"
        android:layout_below="@+id/visualizer3"
        />

</RelativeLayout>